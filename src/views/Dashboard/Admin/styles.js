import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  card: {
    width: '85%',
    margin: '20px auto',
    display: 'flex',
    alignItems: 'center',
    flexWrap: 'wrap',
    padding: '16px 0',
    justifyContent: 'center'
  }
}));
