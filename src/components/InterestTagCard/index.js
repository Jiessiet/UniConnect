import { Card, CardContent, Typography, Container } from "@mui/material";

const InterestTagCard = () => {
  return (
    <Card sx={{ maxWidth: 400 }}>
        <CardContent>
          <Typography variant="h5" component="div" align = "center">
            Deneme
          </Typography>
        </CardContent>
    </Card>
  );
};

export default InterestTagCard;
